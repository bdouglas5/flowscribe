import SwiftUI

enum ColorTokens {
    // MARK: - Backgrounds
    static let backgroundBase = Color(white: 0.07)
    static let backgroundRaised = Color(white: 0.11)
    static let backgroundFloat = Color(white: 0.15)
    static let backgroundHover = Color(white: 0.18)

    // MARK: - Text
    static let textPrimary = Color(white: 0.93)
    static let textSecondary = Color(white: 0.70)
    static let textMuted = Color(white: 0.45)

    // MARK: - Border
    static let border = Color(white: 0.20)

    // MARK: - Buttons
    static let buttonPrimary = Color(white: 0.90)
    static let buttonPrimaryText = Color(white: 0.07)
    static let buttonSecondary = Color(white: 0.18)
    static let buttonSecondaryText = Color(white: 0.90)

    // MARK: - Semantic
    static let statusError = Color(red: 0.75, green: 0.25, blue: 0.25)

    // MARK: - Selection
    static let selectionBackground = Color(white: 0.22)

    // MARK: - Progress
    static let progressTrack = Color(white: 0.15)
    static let progressFill = Color(white: 0.60)
}
